# Production Environment Configuration with Cloudflare R2
# No Code Architect Toolkit

# The name of your application.
APP_NAME=NCAToolkit

# Debug mode setting. Set to `false` for production environments.
APP_DEBUG=false

# Your app's domain or subdomain, without the 'http://' or 'https://' prefix.
APP_DOMAIN=nca.example.com

# Full application URL is automatically configured; no modification required.
APP_URL=https://${APP_DOMAIN}

# SSL settings
SSL_EMAIL=user@example.com

# API_KEY
# Purpose: Used for API authentication.
# Requirement: Mandatory.
API_KEY=your_api_key_here

# Cloudflare R2 Storage Configuration
# Purpose: Cloudflare R2 provides S3-compatible object storage with zero egress fees
# Requirement: Mandatory for R2 setup
#
# How to get these values:
# 1. Go to Cloudflare Dashboard > R2
# 2. Create a bucket (e.g., "nca-toolkit-prod")
# 3. Go to "Manage R2 API Tokens" and create an API token with read/write permissions
# 4. Your endpoint URL can be:
#    - Default: https://<account-id>.r2.cloudflarestorage.com
#    - Custom domain: https://cdn.example.com (if you've set up a custom domain in R2)
# 5. Set your bucket to "Public" in R2 settings if you want direct file access
#
# Important: R2 regions (used for auto-detection):
# - auto (automatic)
# - wnam (Western North America)
# - enam (Eastern North America)
# - weur (Western Europe)
# - eeur (Eastern Europe)
# - apac (Asia-Pacific)
S3_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com
S3_ACCESS_KEY=your_r2_access_key_id
S3_SECRET_KEY=your_r2_secret_access_key
S3_REGION=auto
S3_BUCKET_NAME=nca-toolkit-prod

# Optional: Gunicorn Configuration for production
GUNICORN_WORKERS=4
GUNICORN_TIMEOUT=300

# Optional: Local storage path for temporary files (default: /tmp)
# LOCAL_STORAGE_PATH=/tmp

# Optional: Maximum queue length for background tasks (default: 0 = unlimited)
# MAX_QUEUE_LENGTH=10

# Optional: GCP Cloud Run Job configuration for offloading long-running tasks
# GCP_JOB_NAME=your-cloud-run-job-name
# GCP_JOB_LOCATION=us-central1
